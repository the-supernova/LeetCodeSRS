#flashcards 
![[Pasted image 20220530120149.png]]
?
We use a recursive function with base case if node val == B, where we first push the node val into vector then check for its left and right, if any of them returns true, path exists and we return true else we remove the node val and return false.